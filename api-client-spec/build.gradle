plugins {
    id 'xq-base' version '1.0.0'
    id 'org.springframework.boot' version '3.3.0'
}

group = 'com.xq'
version = '1.0.0'

dependencies {
    implementation(project ':jvm-kit-common')
    implementation 'io.rest-assured:rest-assured:5.5.0'
}

tasks.named('bootJar') {
    enabled = false
}